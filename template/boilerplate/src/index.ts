/**
 * Entry point for {{ componentName }}.
 * Export the main component and types for external use.
 */
export { default } from './{{ componentName }}';
export * from './{{ componentName }}.types';
export const {{ componentName }}Name = '{{ componentName }}';
